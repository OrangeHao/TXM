<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/recharge_item_layout"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@drawable/shape_white_bg_small"
    android:paddingLeft="@dimen/space10"
    android:paddingRight="@dimen/space10"
    android:paddingTop="@dimen/space08"
    android:paddingBottom="@dimen/space08"
    xmlns:tools="http://schemas.android.com/tools"
    >

    <TextView
        android:id="@+id/recharge_price"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:textSize="@dimen/text_size_title"
        android:textColor="@color/textDefualt3"
        tools:text="100元"
        />

</RelativeLayout>